# 复制为 .env 并填写（.env 不入 git）
# 单用户 API token：前端请求需带 Authorization: Bearer <此值>
AICC_API_TOKEN=change-me-to-a-long-random-string
# 百炼 API key（阶段4 LLM 代理用；前端不再持有 key）
DASHSCOPE_API_KEY=
# 本地/部署端口
PORT=5300
